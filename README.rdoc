== SCRAP
 
 git clone https://github.com/vinodprakash/Scrap.git
 git checkout develop
 bundle install
 rake db:migrate
 rails s
 
== Foundation-rails
 
  gem 'foundation-rails' is used in gemfile
  
Then run bundle install
  
You can run the following command to add Foundation:

  rails g foundation:install
