== SCRAP
 
 git clone https://github.com/vinodprakash/Scrap.git
 git checkout develop
 bundle install
 rake db:migrate
 rails s
 
== Gemfile
 
  gem 'foundation-rails' 
  
run 'bundle install'
  
Run the following command in your Terminal:

  rails g foundation:install
